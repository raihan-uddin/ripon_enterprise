; Project-level PHP overrides (read by PHP-FPM and CGI SAPIs).
; Used in development (nginx + PHP-FPM on macOS).
; Cached for `user_ini.cache_ttl` seconds (default 300); restart php-fpm to apply immediately.

max_input_vars = 5000
